# Database Agent Rules

This module encapsulates Mongoose and MongoDB related integrations.

## Rules
- Avoid exporting models directly if abstract interfaces are preferred.
- Schema changes should consider backwards compatibility.
- Ensure all queries use proper indexes to maintain robust performance.
