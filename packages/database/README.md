# Database Module

MongoDB schema configurations and models to interact securely and safely with the primary database. It relies on `mongoose`.

## Usage
Import methods from this package inside `server` or the Next.js `app` routes to interact with the database context.
