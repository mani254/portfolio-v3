# Server backend

Node.js + Express + TypeScript backend. Provides APIs, handles external DB logic that Next.js might not cover directly.

## Development
Run `pnpm dev` inside this module, or `pnpm --filter server dev` from root. The server aims to start on port 8080.
