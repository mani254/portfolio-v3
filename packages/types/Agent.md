# Types Agent Rules

Shared TypeScript definitions across the repository.

## Rules
- Defines generic interfaces, types, and model shapes for the monorepo.
- Do not import dependencies inside this module; keep types purely focused.
