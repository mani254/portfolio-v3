{
  "name": "types",
  "version": "1.0.0",
  "description": "Shared TypeScript definitions and interfaces for the monorepo",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {},
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
