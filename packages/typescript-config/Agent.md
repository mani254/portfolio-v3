# Typescript Config Agent Rules

Shared TS configurations.

## Rules
- Keep strict mode fully enabled (`strict: true`).
- Maintain multiple bases if React environments dictate differing settings vs Node environments.
