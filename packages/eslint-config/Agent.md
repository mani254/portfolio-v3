# ESLint Config Agent Rules

Shared ESLint flat configs.

## Rules
- Modifications here affect the entire monorepo.
- Do not install framework-specific plugins unconditionally; modularize rules across export boundaries (e.g. `next`, `base`, `sanity`).
